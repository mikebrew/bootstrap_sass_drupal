# Bootstrap SASS (Basic)

Watch and compile SASS files using Grunt
`npm install`
`grunt`

Watch SASS files and compile without using Grunt:

`sass --watch src/sass/style.scss:bootstrap_sass/css/style.css`
